# MLProject
```
uv init --bare


uv add scikit-learn pandas numpy joblib matplotlib seaborn


 uv add ipykernel

 uv run python -m ipykernel install --user --name .venv --display-name "eng_env"

 ```
